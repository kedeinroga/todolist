<div class='page'>
  <p style="color: green"><%= notice %></p>

  <h1 class='title'>Todo List</h1>

  <div id="tasks">
    <% @tasks.each do |task| %>
      <%= render task %>
    <% end %>
  </div>

  <div class='button'>
    <%= link_to "New task", new_task_path %>
  </div>
</div>
